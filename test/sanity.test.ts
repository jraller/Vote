import {expect} from 'chai';
import {mount} from 'avoriaz';
// import Sanity from '!vue-loader!./../app/views/sanity.vue';

// const Sanity = require('!vue-loader!./../app/views/sanity.vue');

describe('sanity', () => {
	it('renders a header', () => {
		// const wrapper = mount(Sanity);
		// expect(wrapper/*?*/);
	});
});
